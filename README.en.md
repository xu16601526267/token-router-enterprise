# token-router enterprise backend

This `develop` branch is a backend-only baseline for the next independent interface rebuild. It keeps the Go API relay, tenant hierarchy, API key management, provider/model routing, usage accounting, billing, settlement, operational governance, and background workers.

Legacy interface code has been removed from this branch. New interface work should integrate through the backend APIs.

Use `README.md` as the canonical project description for this branch.
